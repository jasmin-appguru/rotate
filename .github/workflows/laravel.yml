name: Laravel

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  laravel-tests:

    runs-on: ubuntu-latest

    steps:
    - name: Deploy to production
      uses: appleboy/ssh-action@master
      with:
          username: "rotatecdu"
          host: "rotate.clouddownunder.com.au"
          password: "3V6$Rb6ttb9mWgR3"
          script: 'cd /home/rotatecdu/public_html/backend && ./server_deploy.sh'
